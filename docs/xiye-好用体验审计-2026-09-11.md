# xiye「好用」体验重审计 · 2026-09-11

> 审计目标：从**真实用户使用视角**重新评估系统（此前一轮偏安全/商用合规/流程闭环，体验维度未系统扫过）。
> 方法：4 个只读子代理分板块审计（工作流主线 / 第二大脑 / 认证账户连接器 / 通用 UI+设置+组件库），关键 P0 由主代理用真实命令复核。
> 严重度：P0=真实数据丢失/死路/全局白屏；P1=强困惑或明显卡顿；P2=细节/审美/命名。
> 所有 `file:line` 均来自真实文件，未作修改。

## 1. 结论先行

**最该先修的不是 C3（DB 类型契约），而是「全局反馈层 + 一致性组件落地」这两个横向基础件**——它们没落地，导致 80% 的 P1/P2 体验问题重复出现（toast 在多数页面静默、错误被吞、危险操作一半用原生弹窗一半用自研）。把基础件补齐，能一次性消除大量问题，比逐页打补丁性价比高。

**P0 共 5 条（已逐条命令复核）**

| # | 问题 | 铁证 | 影响 |
|---|------|------|------|
| P0-1 | 全局无错误边界，任意异常跌白屏/原生报错 | `app/error.tsx`、`app/global-error.tsx`、`app/not-found.tsx` 均 **MISSING**（已 ls 确认） | 路由级异常/访问不存在路径 → 技术栈报错，无「返回首页」 |
| P0-2 | `<Toaster/>` 只挂在 `/brain`，其他页面 toast 静默 | `components/second-brain.tsx:3252` 是唯一实例；`app/` 下 grep 零匹配（已确认） | `/account` `/workflow` `/login` `/components` 等调用 `toast()` 用户**完全看不到** |
| P0-3 | 注册「假成功」死路：已存在邮箱返回「去查邮件」但永不发信 | `lib/api-handlers/auth/register.ts:50`（error 分支也返回 `requiresEmailConfirmation:true`） | 老用户想重注册 → 看到成功去查邮件 → 永远收不到，无任何报错/补救 |
| P0-4 | 邮箱确认链接 `type=signup` 被错误跳到「设置新密码」页 | `lib/api-handlers/auth/confirm.ts:42,49`（统一 `redirect("/auth/update-password")`，覆盖 signup/email） | 新用户点注册确认邮件 → 落地「重设密码」，心智断裂 |
| P0-5 | collab「重置对话」零确认，一键清空全部访谈+产物 | `app/workflow/components/chat-stream.tsx:329-349`（`reset()` 清 intentSession/productBrief/techStack/visualStyle/blueprint/panelOutput/deliverArtifacts）+ `:561-569` 图标按钮直接 `onReset` 无确认 | 误触即丢整段访谈与全部 AI 产物，zustand persist 写回 localStorage，无撤销 |

## 2. 横向基础件（优先批 · 一次消除大量 P1/P2 根因）

1. **全局 Toaster 提级**：从 `components/second-brain.tsx:3252` 提到 `app/layout.tsx`，使 toast 全站可用（解 P0-2 + 多数「反馈静默」）。
2. **补错误边界**：`app/error.tsx` + `app/global-error.tsx` + `app/not-found.tsx`，统一设计系统卡片承接（解 P0-1）。
3. **统一反馈语言**：全仓弃用 `window.alert` / 原生 `confirm` / `window.prompt`，收敛到 `toast` + 自研 `ConfirmDialog`；消除「toast/alert/小灰字」三套并存。
4. **落地已有但被绕过的组件**：`components/ui/confirm-dialog.tsx`（完整实现却 0 复用）、`components/ingested/empty.tsx`（空状态族 0 引用）、`components/ui/toast.tsx` 补 `warning` 类型 + 图标 + `aria-live` + 延长 error 停留。

> 横向批做完后，分板块 P1/P2 多数变为「补一行 toast / 换一个 ConfirmDialog」的量级，可快速推进。

## 3. 分板块发现（精选，全量见子代理原始报告）

### 3.1 工作流主线
- **P1 阶段门禁两套判据**：`refine-stage.tsx:183-191,373` 内部「下一步」`disabled={overallProgress<80}`（要求产出开发规范），但 `workflow/page.tsx:161-167` 进 build 只要求 `briefReady&&refineReady`（不含 guard）→ 按钮禁用却能从步骤条绕过。
- **P1 匿名用户全链路产物不可见且无透明提示**：`collab-stage.tsx:674,765,871,976,1099` 自动生成 effect 全部 `if(!savedProjectId) return`，未登录用户走完访谈也拿不到蓝图/旅程/规格/原型，仅 collab 顶部一条小 banner，refine/build/deliver 无提示 → 易拿到「偏薄工程包」。
- **P1 中间产物无跨阶段入口**：蓝图/旅程/页面地图/规格/原型查看入口全在 `collab-stage.tsx:1322-1434` JSX 内，进 refine/build/deliver 后想回看必须退回流。
- **P2 会诊「表演性 loading」**：`collab-stage.tsx:190,232-309,348,350` 后端一次性返回却伪造 0→100 进度 + `animate-ping` 发光环 + 调情人称（「老公，技术方案我拍板好了」），与极简/专业调性冲突，且削弱「真实生成中」信任。
- **P2 重置/移除零确认**：`build-stage.tsx:629-636` 区块「移除」无确认即时消失。

### 3.2 第二大脑（Brain）
- **P1 随手记提交后无「AI 整理中/已完成」反馈**：`today-space.tsx:199-200`（后台延迟 6s/15s 回写）、`:351`（仅有 `composeError`），提交后 15s 内界面无任何整理态 → 极易误判「没干活/丢了」。而 `second-brain.tsx:1342` 的 `quickSave` 已有 toast「AI 整理在后台进行」，两处不一致。
- **P1 今日空间无搜索/同步状态入口**：`today-space.tsx:605-610` 仅一个「高级工具」按钮；`SearchPanel` 只在 SecondBrain 渲染（`:2135`）；今日空间全文无 ima/obsidian「同步中/失败」状态 → 用户 80% 时间在此却看不到同步、进不去搜索。
- **P1「撤销」=永久删除无确认**：`today-space.tsx:208-215`（`undoNote` 直接 `DELETE`，catch 静默），而 `note-card.tsx:551-576` 删除用了真正的 `ConfirmDialog`，两处容错标准不统一。
- **P2 双轨命名/入口**：「问我的记忆」(`today-space.tsx:534`) vs 「智能问答」(`search-panel.tsx:262`)，同一 `/api/brain/ask` 两套 UI、两套历史管理。
- **P2 死键 typo**：`today-space.tsx:75` `阅读:1, 阅读读:1`（应为「阅读」）。
- **历史 bug 复核**：「随手记把一条拆多条/丢失原文」**已修复**（`today-space.tsx:180` 单条提交 → `inbox.ts:57-90` 整段 apply → `inbox-process.ts:119` 原文恒存 `content`，AI 改写仅入 `struct`），不列入优先修。

### 3.3 认证 / 账户 / 连接器 / 私人资料
- **P1 注册无「重新发送确认邮件」入口**：`register/page.tsx:65-83` 确认等待页只有「去登录」，无 resend，邮件进垃圾箱即卡死。
- **P1 密码找回失效缺重试闭环**：`update-password.ts:49` 失效返回 `not_authenticated`；`login/page.tsx:155-159` 仅提示「链接已失效」，重新申请动作割裂。
- **P1 三连接器散落三处无聚合入口**：ima 在 `account/page.tsx:296-483`；obsidian 深埋 `second-brain.tsx:2220-2417`；飞书藏在 `FeishuImportPanel.tsx:26`；主导航无「集成/绑定」聚合（P0 之外最影响「好用」的信息架构问题）。
- **P1 ima 已绑定无「更新凭证」入口**：`account/page.tsx:310-318` 已绑只显「已绑定」徽标，换 Key 须先解绑再配。
- **P1 ima 绑定失败透传底层错误**：`account/page.tsx:364` `setImaError(data.detail||data.error)` 直接透传 ima 服务端英文/技术报错，用户不知是 Key 错还是网络问题。
- **P1 私人资料设口令无确认 + 无「忘记不可恢复」提示**：`brain/private-data/page.tsx:496-504` 仅一个 `unlockInput`，`doUnlock` 仅校验 `length<4`；E2E 加密按设计不可恢复，却无风险提示。
- **P1 私人资料删除无二次确认**：`brain/private-data/page.tsx:305-317` `deleteItem()` 直接 `fetch DELETE`，配合 E2E 加密删除即永久不可恢复。
- **P1 未登录进私人资料只显静态报错无跳转**：`brain/private-data/page.tsx:124-127` 仅 `setError("请先登录")`，对比 `account/page.tsx:110-113` 会 `router.replace("/login")`。
- **P2 飞书解绑无确认**、**P2 ima 解绑用原生 confirm**（`:174`）、**P2「自动填充表单（测试）」标签残留生产 UI**（`:365`）。

### 3.4 通用 UI / 设置 / 组件库预览
- **P1 静默 catch 吞错**：`account/page.tsx:98,132,192,245` 等多处 `/* 忽略 */`，网络抖动/接口失败不提示不重试。
- **P1 危险操作原生 confirm 与自研 ConfirmDialog 并存**：`account/page.tsx:174,215` 用 `confirm()`，而 `components/ui/confirm-dialog.tsx` 实现完整却 0 复用。
- **P1 组件库预览 4 处 `window.alert`**：`app/components/page.tsx:1018,1122,1167,1350`（读取项目/下载品牌包/AI 改写失败弹系统框），与精致预览 UI 割裂。
- **P1 ima 解绑后无成功反馈**：`account/page.tsx:173-178` 仅 `setImaBound(false)`，无 toast。
- **P2 表单无内联校验 / 无 `aria-invalid`**：`account/agents/page.tsx:233-249`；全仓 `aria-invalid` 在 `app/` 下 0 命中（按钮样式钩子定义却从未设置）。
- **P2 发光阴影三级跳**：`search-panel.tsx:131`（`shadow-2xl shadow-primary/15`）vs `note-card.tsx:111`（`shadow-sm`）vs `second-brain.tsx:2031`（`shadow-primary/5`），与极简基调违和。
- **P2 组件库定宽硬编码**：`app/components/page.tsx:589,2105` `width:1200` + `flex:"none"`，窄屏有横向溢出风险。
- **P2 复制失败静默**：`copy-button.tsx:32-34` catch 静默，无 clipboard 权限时点了无反应。

## 4. 推荐批次顺序（按「好用」性价比）

| 批次 | 范围 | 价值 | 体量 |
|------|------|------|------|
| **A（横向基础件）** | Toaster 提级 + error 边界 + 统一 feedback(toast/ConfirmDialog) + 启用 Empty 族 + toast warning/aria-live | 一次消除 80% P1/P2 根因 | 中 |
| **B（认证死路）** | P0-3 注册假成功 + P0-4 confirm 分流 + resend + 密码找回重试闭环 + 私人资料未登录跳转 | 解真实死路/数据风险 | 小 |
| **C（工作流防丢+门禁）** | P0-5 重置确认 + 门禁统一 + 匿名链路透明提示 + 产物库入口 | 解数据丢失+强困惑 | 中 |
| **D（第二大脑反馈）** | 随手记整理态 + 今日空间搜索/同步入口 + 撤销确认 | 解「建得快找不着」 | 中 |
| **E（连接器聚合+私人资料容错）** | /account 聚合三连接器 + ima 更新凭证 + 私人资料确认/提示 | 解信息架构+高危容错 | 中 |
| **F（审美/命名收尾）** | 发光阴影/表演性 loading/人称/命名统一/死键/定宽 | 调性统一 | 小 |

**建议**：先 A 后 B（A 是地基，B 含两个 P0 死路），再 C/D/E，F 最后。C3（DB 类型契约）仍建议搁置——它不解决「好用」，且会引爆存量类型错误墙。

## 5. 验证备注
- P0-1/2/3/4/5 已由主代理用 `ls` / `grep` / `Read` 真实复核，file:line 准确。
- 子代理为只读 Explore，未修改任何文件；本报告为纯审计，不含代码变更。
- 分板块 P1/P2 的 file:line 来自子代理读文件所得，抽样复核一致。

---

## 6. 批次 A 执行记录（2026-09-11 完成）

> 范围：横向基础件（P0-1 / P0-2 + 统一反馈地基），全部改动未提交。

### 变更清单

| # | 项 | 文件 | 内容 |
|---|-----|------|------|
| A1 | toast 增强 | `components/ui/toast.tsx` | 新增 warning 类型 + 状态图标（CheckCircle2/XCircle/AlertTriangle/Info）、容器 `aria-live="polite"`、error/warning 停留延长至 6s、点击可关闭；`toast` 以 `Object.assign` 同时提供 `toast(msg, type)` 与 `toast.success/error/warning/info` 快捷方法 |
| A2 | Toaster 提级 | `app/layout.tsx` + `components/second-brain.tsx` | `<Toaster/>` 从 second-brain（:3252 仅 /brain 生效）移到全局 layout body 末尾，全站 toast 可见 —— 修 P0-2 |
| A3 | 错误边界 | `app/error.tsx` / `app/global-error.tsx` / `app/not-found.tsx`（新增） | 段级错误卡（重试+返回首页）、根级兜底（'use client'）、404 页 —— 修 P0-1，不再白屏 |
| A4 | 命令式确认弹窗 | `components/ui/confirm-dialog.tsx` + `app/layout.tsx` | 新增 `confirmDialog(opts): Promise<boolean>`（zustand 队列）+ 全局 `<ConfirmHost/>` 挂 layout；替换 `app/account/page.tsx` 解绑 ima(:174)、删除项目(:215) 两处原生 `confirm`，及 2 处保存失败 `alert`(:584,587) → `toast.error` |
| A5 | 原生弹窗清零 | `app/components/page.tsx`、`app/account/agents/page.tsx` | 4 处下载/AI 改写失败 `window.alert`(:1018,:1122,:1167,:1350) 与 agents 页 3 处 `alert`(:156,:159,:264) 全部替换为 `toast.error` |

### 验证

- `tsc --noEmit` → **exit 0**（三轮：A1–A3 / A4–A5 / 最终，均 0 错误）。
- `eslint` 改动文件 → **0 errors**（21 条 warning 全部为 pre-existing：set-state-in-effect、存量 `any`、layout 字体等，非本批引入）。
- 全仓 `app/**/*.tsx` 扫描 `window.alert|window.confirm|alert(|confirm(` → **0 残留**。

### 设计要点

- `confirmDialog` 采用 zustand 队列而非单例状态：并发多次调用排队渲染（一次只显示首个），resolve 后出队；ESC/遮罩/取消均 resolve(false)。
- `ConfirmHost` 与 `ConfirmDialog` 复用同一 portal 渲染路径（document.body + z-[110]），避免外层容器裁剪的已知坑。
- `toast` 快捷方法以 `Object.assign` 挂在函数上，保持 `toast(msg, type)` 向后兼容，零调用点破坏。

### 遗留

- 组件库复制失败静默（`copy-button.tsx:32-34`）与定宽硬编码属批次 F 范围，未动。
- `Empty` 组件族启用（审计 A 项之一）尚未做 —— 现有空态分散在各页，属批次 C/D 顺带项。

---

## 7. 批次 B 执行记录（2026-09-11 完成）

> 范围：认证死路（P0-3 / P0-4 + 配套闭环），全部改动未提交。

### 变更清单

| # | 项 | 文件 | 内容 |
|---|-----|------|------|
| B1 | 修 P0-3 注册假成功 | `lib/api-handlers/auth/register.ts` | 错误分支不再伪装 `requiresEmailConfirmation`：Supabase 422/already registered → **409 `email_taken`**（前端 `app/register/page.tsx:60` 早有处理，从未被触发过），其余错误如实 500。死路消除：已注册用户被引导去登录/找回密码 |
| B2 | 修 P0-4 confirm 分流 | `lib/api-handlers/auth/confirm.ts` + `app/login/page.tsx` | 按邮件类型分流：`recovery`/`email_change` → `/auth/update-password`；`signup`/`email`/`magiclink`/`invite` 及裸 PKCE `code` → `/login?verified=1`（会话随 cookie 写入）。login 页新增「邮箱已验证，请登录继续」提示 |
| B3 | 确认邮件重发闭环 | `lib/api-handlers/auth/resend.ts`（新增）+ auth 分发器 + `app/register/page.tsx` | 新端点 `POST /api/auth/resend`（`supabase.auth.resend` signup，IP 限流 3 次/分钟，统一成功防枚举）；注册「查收邮件」屏新增 60s 冷却重发按钮。分发器为生成器产物且源 route.ts 已删，按生成器同格式手动补 `m7` 条目 |
| B4 | 私人资料登录跳转 | `app/brain/private-data/page.tsx` | 401 时除提示外加「去登录 →」链接（`/login?next=/brain/private-data`），不再只有一句死文本 |

### 验证

- `tsc --noEmit` → **exit 0**。
- `eslint` 7 个改动文件 → **0 errors**（2 条 warning 为 private-data 页 pre-existing set-state-in-effect，非本批引入）。

### 遗留 / 说明

- `email_taken` 的枚举权衡：找回密码流程本就需要邮箱可达性验证，且 Supabase 注册响应原生含该语义，防枚举收益有限而 UX 死路代价高（审计已批准此取舍）。
- auth 分发器注释仍为 AUTO-GENERATED——本次手动补条目与生成器输出格式一致；生成脚本不可重跑（源 route.ts 已删除，重跑会 `rmSync` 清空 handlers），已在脚本头部语义确认。
- 密码找回闭环已存在（login 页「忘记密码」→ 邮件 → confirm 分流 → update-password），本批通过 B2 分流修正补全最后一环。

---

## 8. 批次 C 执行记录（2026-09-11 完成）

> 范围：工作流防丢 + 门禁统一 + 匿名透明 + 产物库入口，全部改动未提交。

### 变更清单

| # | 项 | 文件 | 内容 |
|---|-----|------|------|
| C1 | 修 P0-5 重置零确认 | `app/workflow/components/chat-stream.tsx` | `reset()` 前置 `confirmDialog`：存在访谈/产品创意/蓝图/交付件时弹「重置对话」破坏性确认（destructive 红钮），空态直清不弹；确认文案明示「不可撤销」 |
| C2 | 门禁统一（P1） | `lib/flow-refine-gate.ts`（新增）+ `app/workflow/page.tsx` + `refine-stage.tsx` | 抽出共享判据 `refineOverallProgress()`（产品愿景 60/100 + 风格 100 + 技术栈 100 + guard 产出 100 均值）：refine「下一步」按钮与页面门禁**同一函数**，堵死「按钮禁用但步骤条绕过」的口子；gate 文案同步更新（明示 80% 要求） |
| C3 | 匿名透明提示（P1） | `components/workflow/local-draft-hint.tsx`（新增）+ refine/build/deliver 三阶段挂载 | 紧凑单行警示条「当前为本地草稿——保存项目后才能自动生成并云端留存后续产物」+ 保存项目按钮（401 → 登录引导），复用 collab 的 captureFlowSnapshot 保存逻辑；collab 保留原大号引导卡不重复 |
| C4 | 产物库入口（P1） | `app/workflow/page.tsx` | 头部常驻「产物库」下拉（Package 图标）：方案会诊产出 / 页面蓝图（N 页）/ 交付产物 三行状态（未生成置灰、已生成可点跳对应阶段）+ 「已保存项目」链接 /account；解决「进 refine/build/deliver 后想回看必须退回流」 |

### 验证

- `tsc --noEmit` → **exit 0**。
- `eslint` 7 个改动文件 → **0 errors**（18 条 warning 全部 pre-existing）。

### 实现备注

- `chat-stream` 组件内只有 store setter 引用，`hasContent` 判定用 `useFlowStore.getState()` 快照读取，不新增订阅、不触发额外重渲染。
- `jumpToArtifact` 初版 useCallback 引用了后文定义的 `goTo`（TDZ 风险），已改为 JSX 内联调用。
- `LocalDraftHint` 自包含保存逻辑（与 collab `handleSaveDraft` 同参数同降级路径），savedProjectId 存在时渲染 null，不与 collab 大卡叠加。

---

## 9. 批次 D 执行记录（2026-09-11 完成）

> 范围：第二大脑反馈（随手记整理态 + 搜索/同步入口 + 撤销确认），全部改动未提交。

### 变更清单

| # | 项 | 文件 | 内容 |
|---|-----|------|------|
| D1 | 随手记「AI 整理中」态 | `components/brain/today-space.tsx` | receipt 项加 `organizing` 态：收录后行内显示 spinner +「AI 整理中…」，6s/15s 延迟刷新拉到 `struct` 即解除，20s 兜底强制解除（整理服务超时不再常驻转圈）——对齐 second-brain quickSave 的后台整理语义 |
| D2 | 搜索/同步入口 | `today-space.tsx` + `brain-home.tsx` + `second-brain.tsx` | 底部入口新增「搜索记忆」按钮（与 Cmd+K 同款面板：切视图→80ms 后广播 `brain:open-search`→SecondBrain 打开）；新增 `SyncStatusLine` 单行同步状态（Obsidian 已连接/未开启 + N 条待同步 → 点进高级工具；ima 已绑定/未绑定 → 进个人中心；未登录/未知不渲染避免误导） |
| D3 | 撤销加确认 | `today-space.tsx` | `undoNote` 前置 `confirmDialog` 破坏性确认（「撤销会永久删除…不可恢复」），失败 `toast.error` 不再静默，删除成功后刷新列表（原实现只移除 receipt 行，最近记录里的笔记残留到下次刷新） |
| D4 | 死键 typo | `today-space.tsx:74` | `SYSTEM_TAG_MAP` 移除 `阅读读` 死键（历史脏 tag 自动走哈希兜底色） |

### 验证

- `tsc --noEmit` → **exit 0**。
- `eslint` 3 个改动文件 → **0 errors**（24 条 warning 全部 pre-existing）。

### 实现备注

- `SyncStatusLine` 为自包含组件：并行 GET `/api/account/ima`（bound）与 `/api/brain/obsidian/config`（enabled + unsyncedCount），非 200 一律置 null 不渲染对应 chip。
- 搜索接线复用既有 window 事件总线模式（同 `brain:today-refresh`），未引入新状态管理；80ms 延迟保证 SecondBrain 挂载并注册监听后再广播。

---

## 10. 批次 E+F 执行记录 + 私人资料功能下线（2026-09-11 完成）

> 用户决策：**私人资料功能整体下线**（「没什么用」）；其余完成批次 E、F。全部改动未提交。

### 私人资料下线（用户拍板）

| 文件 | 处置 |
|------|------|
| `app/brain/private-data/page.tsx`、`app/api/private-data/[[...path]]/route.ts`、`lib/api-handlers/private-data/{index,[id],audit}.ts` | **删除**（均 git 跟踪，可随时恢复） |
| `components/site-nav.tsx` | Brain 组移除「私人资料」导航项 |
| `lib/audit-log.ts` | 移除 `PRIVATE_DATA_*` 审计动作与相关注释 |
| `lib/api-handlers/brain/ask.ts` | 空库/未命中提示移除「私人资料端到端加密」相关说明 |

**刻意保留**：DB 表（`private_data` / `private_data_audit`）与 `lib/db/*` schema 定义、reconcile DDL 不动——数据库数据不破坏，代码层已零引用；后续如需彻底清理可出迁移脚本 drop。

### 批次 E（连接器聚合 + ima 体验）

| # | 项 | 文件 | 内容 |
|---|-----|------|------|
| E1 | ima 更新凭证入口 | `app/account/page.tsx` | 已绑定态在「已绑定」徽标旁加「更新凭证」按钮，换 Key 不再须先解绑 |
| E2 | ima 绑定失败友好化 | `app/account/page.tsx` | 不再透传 ima 服务端英文/技术报错：凭证类错误（401/403/sign/invalid…）映射为「请检查 Client ID 与 API Key（Key 只显示一次）」，其余统一「服务暂不可用，稍后重试」 |
| E3 | 飞书解绑确认 | `components/table/FeishuImportPanel.tsx` | `disconnect` 前置 `confirmDialog`（明示已导入数据不受影响） |
| E4 | 连接器聚合入口 | `app/account/page.tsx` | 新增「连接器与集成」区块：Obsidian（已连接/未开启）与飞书（已授权/未绑定）两行真实状态卡（并行查询，未登录不渲染徽标）+ 去向说明，ima 配置卡归入同区块 |

### 批次 F（审美/命名收尾）

| # | 项 | 文件 | 内容 |
|---|-----|------|------|
| F1 | 发光阴影收敛 | `components/brain/search-panel.tsx` | `shadow-2xl shadow-primary/15` → `shadow-lg`，与 note-card/其他浮层一致 |
| F2 | 命名统一 | `search-panel.tsx`（:178/:187/:266）+ `second-brain.tsx`（:2031/:2139/:2140） | 「智能问答」→「问我的记忆」，与今日空间同一称呼，消除双轨命名 |
| F3 | 复制失败不再静默 | `components/ui/copy-button.tsx` | clipboard API 失败先降级 `execCommand`（http/受限上下文可用），仍失败 `toast.error` 明示 |

**两项待拍板/有意跳过**：
1. **表演性 loading + 人称**（collab-stage 错峰演出、气泡「老公/宝贝」）：属「后宫智囊团」人设功能的核心设计，且人设可在「个人中心 → 人设管理」自定义——删改是产品方向决策而非缺陷修复，**保留现状**，需要的话单独讨论。
2. **组件库 grainy-carousel 定宽 1200**：轮播组件需固定画布渲染（外层已 `overflow-auto` 兜底窄屏），且 589 行是导出生成代码的模板——**不改**。

### 验证

- `tsc --noEmit` → **exit 0**（初次 exit 2 为 `.next/dev/types/validator.ts` 引用已删页面的过期生成物，删除 `.next/{dev/,}types` 缓存后复跑 0 错误；next dev 启动自动重建）。
- `eslint` 8 个改动文件 → **0 errors**（23 warning 全 pre-existing）。

---

## 11. P1 收尾对账（2026-09-11 完成）

> 用户问「那 P1 呢」→ 全量对账 18 条 P1：12 条已在前批修复、3 条随私人资料下线消失、3 条遗漏本批补齐。

### 遗漏 3 条修复记录

| 项 | 文件 | 内容 |
|-----|------|------|
| P1 密码找回失效缺重试闭环 | `app/login/page.tsx` | 「重置链接已失效」死文本 → 内联「重新发送重置邮件」按钮（复用既有 sendReset；邮箱未填时已有「请先填写邮箱」错误引导），一键闭环 |
| P1 静默 catch 吞错 | `app/account/page.tsx` | `loadImaSyncLogs` 失败不再静默：新增 `imaLogsErr` 态 + 日志区域内联「加载失败」提示；进度轮询 catch 补「有意静默」注释（结果/错误另有展示，弹 toast 反打扰）；builder 兜底 catch 属既定降级路径保留 |
| P1 ima 解绑后无成功反馈 | `app/account/page.tsx` | `doImaUnbind` 补 `res.ok` 判定：成功 `toast.success`，失败 `toast.error`（原实现 fire-and-forget，解绑失败也显示已解绑） |

### P1 全量对账表

| 状态 | 条目 | 批次 |
|------|------|------|
| ✅ 已修 | 门禁两套判据 | C2 |
| ✅ 已修 | 匿名产物不可见无透明提示 | C3 |
| ✅ 已修 | 中间产物无跨阶段入口 | C4 |
| ✅ 已修 | 随手记无整理态反馈 | D1 |
| ✅ 已修 | 今日空间无搜索/同步入口 | D2 |
| ✅ 已修 | 撤销=永久删除无确认 | D3 |
| ✅ 已修 | 注册无 resend 入口 | B3 |
| ✅ 已修 | 三连接器无聚合入口 | E4 |
| ✅ 已修 | ima 无更新凭证入口 | E1 |
| ✅ 已修 | ima 绑定失败透传底层错误 | E2 |
| ✅ 已修 | 原生 confirm 与自研弹窗并存 | A4 |
| ✅ 已修 | 组件库 4 处 window.alert | A5 |
| ✅ 本批 | 密码找回重试闭环 | §11 |
| ✅ 本批 | 静默 catch 吞错（日志加载） | §11 |
| ✅ 本批 | ima 解绑无成功反馈 | §11 |
| ➖ 下线 | 私人资料口令无确认 / 删除无二次确认 / 未登录无跳转（×3） | 功能移除 |

### 验证

- `tsc --noEmit` → **exit 0**。
- `eslint` 2 个改动文件 → **0 errors**（2 条 warning 为 pre-existing set-state-in-effect）。
- 过程插曲：改进度轮询注释时误删 `}, 800);`（setInterval 收尾），grep 核验后已补回——Edit 后 Read/Grep 核验惯例再次生效。
