'use client';

import WexoSection from "./shared";

// Wexo 整站区块「Unique Feature」（Originkit 原版，CSS 动效忠实还原）。
export default function WexoUniqueFeature() {
  return <WexoSection slug="unique-feature" />;
}
