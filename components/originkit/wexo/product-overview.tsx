'use client';

import WexoSection from "./shared";

// Wexo 整站区块「Product Overview」（Originkit 原版，CSS 动效忠实还原）。
export default function WexoProductOverview() {
  return <WexoSection slug="product-overview" />;
}
