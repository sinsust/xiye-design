'use client';

import WexoSection from "./shared";

// Wexo 整站区块「User Feedback」（Originkit 原版，CSS 动效忠实还原）。
export default function WexoUserFeedback() {
  return <WexoSection slug="user-feedback" />;
}
